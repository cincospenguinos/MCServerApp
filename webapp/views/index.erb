<%= erb :header_file %>
<div class="jumbotron">
  <div class="container">
    <h1>Andre's Minecraft Server</h1>
    <p>This is Andre's Minecraft Server. You can startup the server by using the panel above, or request access
    to the server <a href="#request-access-space">here</a>.</p>
  </div>
</div>
<div class="container container-padding">
  <div class="row">
    <div id="minecraft-screenshots-carousel" class="carousel slide" data-ride="carousel">
      <ol class="carousel-indicators">
        <% i = 0 %>
        <% @screenshots.each do |image| %>
          <li data-target="#minecraft-screenshots-carousel" data-slide-to='<%= i.to_s %>' <%= 'class="active"' if i == 0 %></li>
          <% i += 1 %>
        <% end %>
      </ol>

      <div class="carousel-inner" role="listbox">
        <% i = 0 %>
        <% @screenshots.each do |image| %>
          <div class="item <%= 'active' if i == 0 %>">
            <img src="<%= image.gsub('public/', '') %>" width="100%" />
          </div>
          <% i += 1 %>
        <% end %>
      </div>
    </div>
  </div>
</div>
<div class="container container-padding">
  <div class="row">
    <div class="col-md-4">
      <h2>Map</h2>
      <p>We have a map of our server showing off the cool stuff.</p>
      <p><a class="btn btn-default" href="map" role="button">View map &raquo;</a></p>
    </div>
    <div class="col-md-4">
      <h2>Generic Stats</h2>
      <p>See the stats of the various members of our server.</p>
      <p><a class="btn btn-default" href="stats" role="button">View stats &raquo;</a></p>
    </div>
    <div class="col-md-4">
      <h2>Code</h2>
      <p>View all of my code on GitHub</p>
      <p><a class="btn btn-default" href="https://github.com/cincospenguinos/MCServerApp" role="button">View code &raquo;</a></p>
    </div>
  </div>
</div>
<div class="container container-padding">
  <div class="row">
    <div class="col-md-12">
      <h2>What is Andre's Server?</h2>
      <p>Andre's server is a simple Minecraft world. It is a private server, but anyone may join by requesting.</p>
      <h2>How do I join?</h2>
      <p><a href="#request-access-space">See below.</a></p>
      <h2>I got on but I'm not in your world. Why is that?</h2>
      <p>Andre manages two Minecraft servers: one that is open to the public, and one that is for private use only. This
      web page is designed for that private server. It uses a white list to decide who is and isn't allowed on, hence to
      join you must fill out the form below.</p>
      <h2>I'm able to join your server. How do I do that from the lobby world?</h2>
      <p>Simply type the command <pre>/server lafleur</pre> to join my server.</p>
    </div>
  </div>
</div>
<div class="container container-padding">
  <div class="row" id="request-access-space">
    <div class="col-md-8">
      <h2>Request Access</h2>
      <p>Want to play on Andre's server? Request access here!</p>
    </div>
    <div class="col-md-4">
      <form>
        <div class="form-group">
          <input type="text" placeholder="Username" id="access-request-username" class="form-control">
        </div>
        <div class="form-group">
          <input type="password" placeholder="Password" id="access-request-password" class="form-control">
        </div>
        <div class="form-group">
          <input type="text" placeholder="Email" id="access-request-email" class="form-control">
        </div>
        <button type="button" id="submit-access-request" class="btn btn-success">Send Request</button>
      </form>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12" id="access-request-alert-space">
    </div>
  </div>
</div>
<%= erb :footer_file %>